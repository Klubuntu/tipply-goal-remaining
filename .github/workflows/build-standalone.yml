name: Build Standalone

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

permissions:
  contents: write

jobs:
  build-macos:
    name: Build macOS x64 + arm64
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v2
        with:
          version: 10.33.0
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'pnpm'
      - run: pnpm install
        working-directory: app
      - run: pnpm run build:macos
        working-directory: app
      - run: pnpm run build:macos-arm64
        working-directory: app
      - name: Package macOS artifacts (executables only)
        run: |
          mkdir -p release
          for f in app/dist/tipply-gr-macos-*; do
            if [ -d "$f" ]; then
              name=$(basename "$f")
              exe="$f/$name"
              if [ -f "$exe" ]; then
                zip -j "release/${name}.zip" "$exe"
              fi
            fi
          done
          ls -la release/
      - uses: actions/upload-artifact@v4
        with:
          name: tipply-gr-macos
          path: release/*.zip

  build-linux:
    name: Build Linux x64
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v2
        with:
          version: 10.33.0
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'pnpm'
      - run: pnpm install
        working-directory: app
      - run: pnpm run build:linux
        working-directory: app
      - name: Package Linux artifacts (executables only)
        run: |
          mkdir -p release
          for f in app/dist/tipply-gr-linux-*; do
            if [ -d "$f" ]; then
              name=$(basename "$f")
              exe="$f/$name"
              if [ -f "$exe" ]; then
                zip -j "release/${name}.zip" "$exe"
              fi
            fi
          done
          ls -la release/
      - uses: actions/upload-artifact@v4
        with:
          name: tipply-gr-linux
          path: release/*.zip

  build-windows:
    name: Build Windows x64
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v2
        with:
          version: 10.33.0
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'pnpm'
      - run: pnpm install
        working-directory: app
      - run: pnpm run build:windows
        working-directory: app
      - name: Package Windows artifacts (executables only)
        shell: pwsh
        run: |
          $releaseDir = Join-Path (Get-Location) 'release'
          New-Item -ItemType Directory -Force -Path $releaseDir | Out-Null
          $appDistDir = Join-Path (Get-Location) 'app' 'dist'
          Write-Host "App dist directory: $appDistDir"
          $items = Get-ChildItem -Path $appDistDir -Filter 'tipply-gr-win-*' -Directory
          Write-Host "Found directories: $($items | ForEach-Object { $_.Name })"
          foreach ($item in $items) {
            $name = $item.Name
            $exe = Join-Path $item.FullName "$name.exe"
            Write-Host "Looking for executable: $exe"
            if (Test-Path $exe) {
              $zipPath = Join-Path $releaseDir "$name.zip"
              Write-Host "Creating zip: $zipPath"
              Compress-Archive -Path $exe -DestinationPath $zipPath -Force
              Write-Host "Created zip with size: $(Get-Item $zipPath).Length bytes"
            } else {
              Write-Host "Executable not found at: $exe"
              Write-Host "Contents of $($item.FullName):"
              Get-ChildItem -Path $item.FullName -Recurse | ForEach-Object { Write-Host $_.Name }
            }
          }
          Write-Host 'Final release directory contents:'
          Get-ChildItem -Path $releaseDir -Recurse | ForEach-Object { Write-Host $_.FullName }
      - uses: actions/upload-artifact@v4
        with:
          name: tipply-gr-windows
          path: release/*.zip

  create-tag:
    name: Create Tag and Release
    needs: [build-macos, build-linux, build-windows]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Set version tag
        id: set_tag
        run: |
          VERSION=$(node -p "require('./package.json').version")
          echo "TAG=v${VERSION}" >> $GITHUB_ENV
          if ! git rev-parse "v${VERSION}" >/dev/null 2>&1; then
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git tag "v${VERSION}"
            git push origin "v${VERSION}"
          fi

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ env.TAG }}
          name: ${{ env.TAG }}
          files: |
            artifacts/tipply-gr-macos/*.zip
            artifacts/tipply-gr-linux/*.zip
            artifacts/tipply-gr-windows/*.zip
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Update release body
        run: |
          NOTES=$(printf "Release %s\n\nBuild & Setup links:\n- Build: https://github.com/%s/blob/main/README.md#build\n- Setup: https://github.com/%s/blob/main/README.md#setup\n- Themes: https://github.com/%s/blob/main/README.md#themes" "${TAG}" "${GITHUB_REPOSITORY}" "${GITHUB_REPOSITORY}" "${GITHUB_REPOSITORY}")
          gh release edit "${TAG}" -n "$NOTES"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ env.TAG }}
